<h1>Register</h1>

<div class="register">
    <%= form_tag(attempt_register_path, method: :post) do %>
    <table>
        <tr>
            <td><%= label_tag(:username) %></td>
            <td><%= text_field_tag(:username, params[:username]) %></td>
        </tr>
        <tr>
            <td><%= label_tag(:password) %> </td>
            <td><%= password_field_tag(:password) %> </td>
        </tr>
        <tr>
            <td><%= label_tag(:password_confirm) %></td>
            <td><%= password_field_tag(:password_confirm) %></td>
        </tr>
        <tr>
            <td><%= label_tag(:email) %></td>
            <td><%= email_field_tag(:email) %></td>
        </tr>
             <tr>
            <td><%= label_tag(:first_name) %></td>
            <td><%= text_field_tag(:first_name) %></td>
        </tr>
        <tr>
            <td><%= label_tag(:last_name) %></td>
            <td><%= text_field_tag(:last_name) %></td>
        </tr>  
        <tr>
          <td>&nbsp;</td>
          <td><%= submit_tag("Register") %></td>
        </tr>
    </table>
    <% end %>
</div>
